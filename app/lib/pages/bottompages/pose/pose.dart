import 'package:flutter/material.dart';

class PosePosition extends StatelessWidget {
  const PosePosition({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}
